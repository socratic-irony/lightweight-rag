# Core dependencies for lightweight-rag
# Python 3.10+ required (see script header comment)

# PDF processing
PyMuPDF>=1.23.0,<2.0.0

# HTTP client for API requests (Crossref, OpenAlex)
httpx>=0.25.0,<1.0.0

# OpenAI client (used for OpenRouter-compatible APIs)
openai>=1.0.0,<2.0.0

# BM25 ranking algorithm
rank-bm25>=0.2.2,<1.0.0

# Progress bars
tqdm>=4.66.0,<5.0.0

# Environment variable loading
python-dotenv>=1.0.0,<2.0.0

# YAML configuration file support
PyYAML>=6.0,<8.0

# Optional dependencies for semantic reranking
# To enable CPU-based semantic reranking with sentence-transformers, install:
# pip install numpy>=1.24.0,<3.0.0 sentence-transformers>=2.2.0,<3.0.0
# 
# Note: These optional dependencies significantly increase download size and are not
# required for basic functionality. The script gracefully handles their absence.
